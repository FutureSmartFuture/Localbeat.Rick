SCOPED EDITS — ALWAYS-ON PROFILE RULE
=====================================

Paste the block below into the Claude desktop app under:
  Settings → Profile → personal preferences / custom instructions
(the field labeled something like "What personal preferences should Claude consider").

This makes the rule load on EVERY conversation, regardless of whether the
scoped-edits skill triggers. It's the belt-and-suspenders backup to the skill.

--- PASTE THIS ---

Scoped edits — always on. When revising anything that already exists (website,
template, design, doc, deck, spreadsheet, or code), change ONLY the section my
request pertains to. Never rebuild, regenerate, or "clean up" the rest — leave all
unrelated content, structure, and formatting untouched. If a change truly requires
touching something shared (a global variable, nav/header, layout wrapper, a
depended-on formula), make what you safely can, then STOP and ask me before
touching the shared thing. After each edit, give me a one- or two-line note of
exactly what changed, confirming the rest is untouched.

--- END PASTE ---
